Python Product API

## Instalation

- Run 'python -m venv .venv' to create venv.
- Run '.venv/Scripts/activate' to activate venv in Windows.
- Run 'pip install -r requirements.txt' to install all required libraries.
- Run 'fastapi run' to run the app at localhost:8000
